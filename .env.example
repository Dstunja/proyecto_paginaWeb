# Copiar como .env (esta en .gitignore) o definir estas variables en el panel
# del hosting. Se leen EN TIEMPO DE BUILD.
#
# Hoy el sitio no necesita ninguna: los dos mapas usan Leaflet con teselas de
# OpenStreetMap, que no piden API key, y la geocodificacion se hace una sola vez
# con `npm run geocodificar` (Nominatim, tambien sin key).
#
# Este archivo queda como plantilla por si mas adelante se conecta algun
# servicio que si necesite credenciales, por ejemplo un endpoint real para los
# formularios de contacto y PQRS.

# --- Resenas de Google (todavia NO conectado) ---------------------------------
# La seccion "Resenas reales en Google" del inicio se alimenta hoy de
# src/data/reseñas.ts (archivo local, datos de ejemplo). Para traerlas de la API
# real hay que crear un proyecto en Google Cloud con facturacion, habilitar
# "Places API (New)" y definir estas dos variables. Sin prefijo PUBLIC_: la
# consulta se hace en el build, la key nunca debe llegar al navegador.
# El paso a paso completo esta en el comentario de cabecera de src/data/reseñas.ts.
# GOOGLE_PLACES_API_KEY=
# GOOGLE_PLACE_ID=
