# Archivo ESTATICO: se copia tal cual desde public/ a los dos despliegues, asi
# que la URL del sitemap no puede depender del entorno. Se deja apuntando al
# despliegue de Vercel, que es el unico donde este archivo se sirve en la raiz
# del dominio (https://dstunja.com/robots.txt).
#
# En GitHub Pages el sitio cuelga de /proyecto_paginaWeb, asi que este archivo
# queda en /proyecto_paginaWeb/robots.txt: los buscadores solo leen el robots.txt
# de la raiz del dominio, por lo que alli este archivo se ignora y no estorba.
#
# Si algun dia hace falta que la URL del sitemap cambie con el entorno, se
# borra este archivo y se genera con un endpoint (src/pages/robots.txt.ts) que
# tome la URL de `Astro.site`, que ya es condicional en astro.config.mjs.
#
# Si el dominio oficial cambia, hay que cambiar a mano la linea Sitemap de abajo
# ademas de SITIO_VERCEL en astro.config.mjs.

User-agent: *
Allow: /

Sitemap: https://dstunja.com/sitemap-index.xml
