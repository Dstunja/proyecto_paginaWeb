---
import { testimonio } from '../data/site';
import Icono from './Icono.astro';

const iniciales = testimonio.nombre
  .split(' ')
  .map((p) => p[0])
  .slice(0, 2)
  .join('');
---

<figure
  class="mt-8 grid grid-cols-1 items-center gap-8 rounded-card bg-primary px-8 py-9 text-white tablet:grid-cols-[1.4fr_.6fr] tablet:px-11"
>
  <div>
    <div class="mb-4 flex gap-1" role="img" aria-label={`${testimonio.estrellas} de 5 estrellas`}>
      {
        Array.from({ length: 5 }).map((_, i) => (
          <Icono
            nombre="star"
            class={
              i < testimonio.estrellas
                ? 'h-[18px] w-[18px] text-accent [&_svg]:fill-current'
                : 'h-[18px] w-[18px] text-white/30'
            }
            interactivo={false}
          />
        ))
      }
    </div>

    <blockquote class="m-0 font-display text-xl leading-snug font-semibold text-white">
      «{testimonio.texto}»
    </blockquote>

    <figcaption class="mt-5 text-sm text-white/70">
      <span class="font-semibold text-white">{testimonio.nombre}</span> ·
      {testimonio.negocio}, {testimonio.ciudad}
    </figcaption>
  </div>

  <div class="flex justify-start tablet:justify-end">
    {
      testimonio.foto ? (
        <img
          src={testimonio.foto}
          alt={`${testimonio.nombre}, ${testimonio.negocio}`}
          class="h-32 w-32 rounded-full border-4 border-white/20 object-cover"
        />
      ) : (
        /* EDITAR AQUÍ: al cargar la foto real en `testimonio.foto`
           (src/data/site.ts) se reemplazan estas iniciales. */
        <div class="flex h-32 w-32 items-center justify-center rounded-full border-4 border-white/20 bg-secondary font-display text-4xl font-bold text-white">
          {iniciales}
        </div>
      )
    }
  </div>
</figure>
